import { Example } from './example';

export const title = 'How does React bailout work in reconciliation?'

## 예제

<Example />

왜 strict mode에서 D만 두 번 호출되지? C는?

## markUpdateLaneFromFiberToRoot

setState -> dispatchSetState -> scheduleUpdateOnFiber -> markUpdateLaneFromFiberToRoot

